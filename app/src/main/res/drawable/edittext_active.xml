<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Transparent background -->
    <stroke
        android:width="1dp"
        android:color="@color/button_orange"
        android:top="0dp"
        android:left="0dp"
        android:right="0dp"
        android:bottom="1dp" /> <!-- Bottom underline -->
</shape>
